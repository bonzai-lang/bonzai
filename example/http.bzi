require "std:http"
require "std:natives"

let port = 8000
let server = spawn HTTPServer(port)

server->listen(server, fn(req) => {
  req.respondText("text/html", "<h1>Hello, world!</h1>")
})

print("Server running on port $port")
