require "std:http"
require "std:natives"
require "std:json"
require "std:parser"

let url = "https://jsonplaceholder.typicode.com/todos/1"
let res = fetch(url)

match res {
  case Ok(response) => print(response.getJSON)
  case Error(err) => print(err)
}