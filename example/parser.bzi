require "std:parser"
require "std:natives"

let path = "example/parser.bzi,test"

print(path)

let p = Parser::map(
  many1(noneOf([','])).sepBy(char(',')),
  fn(xs) => List::map(xs, implode)
)

print(p.runParser(path))