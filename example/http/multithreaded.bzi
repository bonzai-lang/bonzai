require "std:foundation"

mut i = 0

while true {
  print("Iteration: $i")
  i += 1

  let thread = Thread::newDetached(fn() => {
    print("Thread iteration: $i")
  })
  
  print("Thread started: $thread")
  print(i)
}