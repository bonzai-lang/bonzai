require "std:http": HTTP::create, get_path, send_buffer, redirect_to
require "std:foundation"

let thread = spawn HTTP::create(
  fn(req) => {
    req.send_buffer("Hello, world!")
    
    true
  },
  8080
)

print("Server started on port 8080")

thread.wait()